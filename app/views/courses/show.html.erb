<% if @program %>
  <div id="top_cont">
      <%= courses(@program) %>
      <%= photo(@program) %>
  </div>
<% end %>

<h2><%= @course.name %></h2>


<%= @course.description %>

<%= link_to 'редактировать', edit_admin_l_course_path(lang_id(@lang),@course), :class => "admin_edit_link"  if admin? %>
